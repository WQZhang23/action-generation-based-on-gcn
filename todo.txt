Mon. Data loader and processor
      - data struction
      - mask generation (random)

Tue. Model
      - generation model
      - stage1,2
      - layer [gated conv.]
      - loss function


      Gen:
      - stage1:   input (x, mask)
                  2 conv. -- down conv. -- conv. -- down conv -- 2 conv. -- up conv. -- conv -- up conv. -- 2 conv.
      - stage2:   input (x_pro, mask)
                  2 conv. -- down conv. -- 2conv. -- up conv. -- 2 conv.

      Dis: (not using gate conv.)
      2 conv. -- down conv. -- 2 conv.





Wen. 1. loss function [generation.py]
        model and training config --> [generation_train.yaml]
     2. Visulazition
     3. Kinetic data set channel is 3 (x,y,confidence), in our case --> we do not generate the confidence ?


further setting
- Kinetic data set channel is 3 (x,y,confidence)/ 2 (x,y)
- discriminator final activation layer [now-->self.fcn, but without sigmoid?] + loss function
- gate layer [on/off] [input channel+1 / channel+2] [laster channel as y or other option]

Tur. Data augmentation in Recognition
     [load weight]
     [generation.py --> test()]
      - args
      - sys
      [on/off gate layer]

      - learning rate [generation.py --> adjust_lr() , now: gen and dis lr change sim.tly]
      - loss function of GAN [generation.py --> train(), #gan loss ... , now:]

~ - feeder/feeder_generation.py

- config/st_gcn/kinetics-skeleton/generation_train.yaml
- processor/generation.py
- net/graph_gen.py



paper writing
- data augmentation
  - assumption []
  - 

- network
  - graph process the spatial and temporal data
  - gate conv. layer

- data preprocessing
  - feeder [feeder_generation.py; random_mask --> window_size]
  - feeder [len of the active (non-zero) data; ]


python main.py generation -c config/st_gcn/kinetics-skeleton/generation_train.yaml
python main.py generation -c config/st_gcn/ntu-xsub/generation_train_local.yaml
sudo docker run -ti --gpus '"device=0"' --shm-size=8192m -v ~/Workspace:/home/wuqiang/Workspace gcn:v1
sudo docker run -ti --gpus '"device=0,1,2,3"' --shm-size=8192m -v /home/wuqiang/link_dataset/data/:/data/ -v /home/wuqiang/Workspace/4_generative_model/2_DA_Gesture:/code/ gcn:v1